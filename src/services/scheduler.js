const minutesFor = (difficulty) => ({ 1: 20, 2: 30, 3: 45 }[difficulty] || 30);
const categoryWeight = (category) => ({ technical: 0, "system-design": 1, behavioural: 2, "company-fit": 3 }[category] ?? 4);
const focusFor = (questions) => { const categories = [...new Set(questions.map((question) => question.category))]; if (categories.includes("technical")) return "Technical foundations"; if (categories.includes("system-design")) return "System design"; if (categories.includes("behavioural")) return "Behavioural preparation"; return "Company and role fit"; };
export const buildSchedule = (interviewDays, questions) => { const days = Math.max(1, Math.min(Number(interviewDays) || 1, questions.length || 1)); const buckets = Array.from({ length: days }, () => []); [...questions].sort((a, b) => categoryWeight(a.category) - categoryWeight(b.category)).forEach((question, index) => buckets[index % days].push(question)); return buckets.map((questionsForDay, index) => ({ day: index + 1, focus: focusFor(questionsForDay), estimated_minutes: questionsForDay.reduce((total, question) => total + minutesFor(question.difficulty), 0), question_ids: questionsForDay.map((question) => question.id) })); };
